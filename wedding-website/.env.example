# Google Sheets RSVP Configuration (set in Vercel dashboard under Environment Variables)
#
# 1. Create a Google Sheet with headers: Timestamp, Name, Email, Phone, Guest_Count, Event, Side, Group_Name, Message
# 2. Go to Extensions > Apps Script
# 3. Paste the code from google-apps-script.js in this repo
# 4. Deploy > New deployment > Web app > "Anyone" access
# 5. Copy the Web App URL
#
# IMPORTANT: This is a server-side variable (used by Vercel serverless functions).
# Do NOT prefix with VITE_ — it should NOT be exposed to the browser.

GOOGLE_SCRIPT_URL=your_google_apps_script_url_here
